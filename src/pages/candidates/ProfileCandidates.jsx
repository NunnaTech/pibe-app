import { PrincipalContentProfile } from '../../components/profile_candidates_components/PrincipalContentProfile';
import { NavBarApp } from '../../components/navbar/NavBarApp';

export const ProfileCandidates = () => {
	return (
		<>
			<NavBarApp />
			<PrincipalContentProfile />
		</>
	);
};
